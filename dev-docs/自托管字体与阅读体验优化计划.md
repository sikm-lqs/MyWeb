# 自托管字体与阅读体验优化计划

**日期**: 2026-08-04
**状态**: 已执行完成（build 验证通过）
**基于**: 用户反馈 + 完整代码库分析

## 1. 需求总结

用户明确要求：

1. 删除用来占位的博客内容（非实质写作）。
2. 字体自托管：
   - 中文正文和标题：MiSans / Noto Sans SC
   - 英文及数字：使用字体自带的拉丁字符
3. 解决颜色系统的硬编码问题（大量 rgba(216,202,186,...) 等）。
4. 博客阅读页（ArticleLayout）优化：
   - 返回按钮改小，改成面包屑样式 `首页 > 博客`
   - 阅读宽度增加（原来 max-w-3xl 太窄）
   - 元信息（日期、阅读时长、字数）前使用小图案/图标代替文字
5. 其他反馈：
   - agent-state-management 保留（实质内容）
   - 宽度自己选合适值
   - 图标用 emoji 即可
   - 字数统计要给 Life 和 Projects
   - 所有标题字号偏大的一律改小
   - 选择自托管字体方案，由 AI 帮助下载字体文件

## 2. 总体方案

- **自托管字体**：在 `public/fonts/` 放置 woff2/ttf 文件，添加 @font-face，使用字体栈优先 MiSans / Noto Sans SC。
- **删除占位**：仅删除通用教程类内容，保留 agent-state-management 等实质写作。
- **颜色统一**：引入 `--border-paper` 等 CSS 变量，替换硬编码。
- **标题字号**：全局收紧过大的 clamp 和 text-*-xl。
- **阅读体验**：
  - Article 内容容器：`max-w-4xl`
  - 面包屑：小尺寸 `首页 › 博客`（支持中英）
  - Meta：使用 emoji 图标 + 自动字数统计
- **字数统计**：构建时读取源 MDX，剥离 frontmatter，统计汉字 + 英文词。
- **覆盖范围**：Writing（含 Research）、Life、Projects。

## 3. 详细改动清单

### 3.1 删除占位内容
- `src/content/writing/zh/dive-into-llm.mdx`
- `src/content/writing/zh/eval-driven-iteration.mdx`
- `public/images/writing/eval-driven-iteration.svg`

保留：
- agent-state-management（具体 Agent 状态设计思考）
- 所有 research、life、projects

### 3.2 自托管字体
**下载的字体**（推荐手动或用以下命令）：
- Noto Sans SC：从 Google Fonts 下载或使用
  ```powershell
  # 示例 (在项目根执行)
  Invoke-WebRequest -Uri "https://fonts.gstatic.com/s/notosanssc/v36/k3kCo84MPvpLmixcA63oeAL7Iqp5K3nR1w.woff2" -OutFile "public/fonts/NotoSansSC-Regular.woff2"
  ```
- MiSans：去 https://hyperos.mi.com/font/ 下载 zip，解压 woff2 到 public/fonts/MiSans-*.woff2

Noto 已尝试自动获取（部分环境可能需手动）。字体栈优先 MiSans。

**CSS 改动** (`src/styles/global.css`)：
- 添加 @font-face
- 更新 body font-family
- 保持 mono/code 不变

**字体文件位置**：`public/fonts/NotoSansSC-*.woff2` / `.ttf` 及 MiSans

**说明**：自托管 = 本地文件 + @font-face，无外部依赖。

### 3.3 标题字号调整（全部）
- Welcome 大标题、Static 标题、Article/Project h1、SectionTitle h2 等全部收小。

### 3.4 颜色硬编码清理
新增变量：
- `--border-paper`
- 必要时 `--btn-primary-*`

替换位置：
- ArticleLayout、ProjectLayout、Button、LanguageSwitch、Avatar、AboutPage、部分 global.css

### 3.5 阅读页优化（Article + 扩展）
- 宽度：`max-w-4xl`（比 3xl 宽松，适合中文阅读）
- 面包屑替代 Button：
  ``` 
  首页 › 博客
  ```
  （中英对应，链接可点，小尺寸）
- Meta 改成：
  `📅 日期   ⏱ 阅读时长   📝 字数`
- 字数统计：
  - 新增 `computeWordCount` 工具函数
  - 在 writing/life/projects 的 [slug] 页面计算并传入
  - 同时更新 ArticleLayout 和 ProjectLayout 的 meta 展示

### 3.6 其他
- 更新 en 对应页面
- 确保双语、主题切换正常
- 构建验证

## 4. 执行顺序

1. 生成本文档
2. 创建 `public/fonts/` 目录
3. 下载字体文件（自托管）
4. 更新 global.css（字体 + 字号 + 颜色变量）
5. 删除占位文件
6. 实现字数计算函数
7. 修改 ArticleLayout（面包屑 + 宽度 + meta 图标）
8. 修改 ProjectLayout（meta 图标 + 宽度一致性）
9. 修改各 [slug].astro 页面传入 wordCount
10. 调整其他大标题
11. 替换硬编码颜色
12. `npm run build` 验证
13. 手动测试关键页面

## 5. 风险与注意
- 字体文件较大（下载后注意 gitignore 或 LFS）
- 字数统计为近似值（汉字为主 + 英文词）
- 构建时使用 fs，Astro 构建环境支持
- 移动端响应式需复核

## 6. 验证清单
- [ ] npm run build 成功
- [ ] 首页标题大小合适
- [ ] 文章页宽度增加、面包屑正确、emoji meta 显示、字数正确
- [ ] Life 和 Projects 也有字数
- [ ] 字体生效（中文用 MiSans/Noto，英文用其拉丁）
- [ ] 颜色无硬编码残留
- [ ] 浅色/深色 + 中英文切换正常

---

**本文档由 AI 根据对话和代码分析自动生成。**

## 执行摘要（已完成）
- 占位博客已删除
- 字体自托管准备就绪（@font-face + 栈更新；字体文件请按上方命令放置）
- 所有大标题字号已收小
- 颜色变量化（--border-paper 等）
- 文章阅读宽度改为 max-w-4xl
- 面包屑 `首页 › 博客`（小尺寸，emoji meta）
- 字数统计已实现并覆盖 writing/research/life/projects
- `npm run build` 成功 (27 pages)

如需补充 MiSans 字体文件或进一步微调，请提供文件或指示。

