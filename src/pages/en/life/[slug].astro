---
import { getCollection, render } from "astro:content";
import ArticleLayout from "../../../layouts/ArticleLayout.astro";
import { getCollectionPath } from "../../../i18n/utils";
import {
  getEntryPath,
  getTranslatedEntry,
  computeWordCount,
} from "../../../lib/content/translations";

export async function getStaticPaths() {
  const entries = await getCollection(
    "life",
    ({ data }) => !data.draft && data.lang === "en",
  );

  return entries.map((entry) => ({
    params: { slug: entry.data.routeSlug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const translation = await getTranslatedEntry("life", entry, "zh-CN");
const wordCount = await computeWordCount("life", "en", entry.data.routeSlug);
---

<ArticleLayout
  title={entry.data.title}
  description={entry.data.summary}
  lang="en"
  currentPath={Astro.url.pathname}
  translationHref={translation ? getEntryPath("life", "zh-CN", translation) : getCollectionPath("life", "zh-CN")}
  collection="life"
  publishedAt={entry.data.publishedAt}
  updatedAt={entry.data.updatedAt}
  readTime={entry.data.readTime}
  tags={entry.data.tags}
  cover={entry.data.cover}
  coverAlt={entry.data.coverAlt}
  backHref={getCollectionPath("life", "en")}
  wordCount={wordCount}
>
  <Content />
</ArticleLayout>
