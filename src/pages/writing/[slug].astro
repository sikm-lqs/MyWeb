---
import { getCollection, render, type CollectionEntry } from "astro:content";
import ArticleLayout from "../../layouts/ArticleLayout.astro";
import {
  getCollectionPath,
} from "../../i18n/utils";
import {
  getEntryPath,
  getTranslatedEntry,
  computeWordCount,
} from "../../lib/content/translations";

export async function getStaticPaths() {
  const entries = await getCollection(
    "writing",
    ({ data }) => !data.draft && data.lang === "zh-CN",
  );

  return entries.map((entry) => ({
    params: { slug: entry.data.routeSlug },
    props: { entry },
  }));
}

const { entry } = Astro.props as { entry: CollectionEntry<"writing"> };
const { Content } = await render(entry);
const translation = await getTranslatedEntry("writing", entry, "en");
const wordCount = await computeWordCount("writing", "zh-CN", entry.data.routeSlug);
---

<ArticleLayout
  title={entry.data.title}
  description={entry.data.summary}
  lang="zh-CN"
  currentPath={Astro.url.pathname}
  translationHref={translation ? getEntryPath("writing", "en", translation) : getCollectionPath("writing", "en")}
  collection="writing"
  publishedAt={entry.data.publishedAt}
  updatedAt={entry.data.updatedAt}
  readTime={entry.data.readTime}
  tags={entry.data.tags}
  cover={entry.data.cover}
  coverAlt={entry.data.coverAlt}
  backHref={getCollectionPath("writing", "zh-CN")}
  wordCount={wordCount}
>
  <Content />
</ArticleLayout>
